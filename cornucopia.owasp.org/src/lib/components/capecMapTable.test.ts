import { describe, it, expect } from 'vitest';

// Skipping component test until Vitest DOM environment (jsdom/happy-dom) and Svelte compiler are configured
describe.skip('CapecMapTable Component', () => {
  it('should render the table correctly', () => {
    expect(true).toBe(true);
  });
});